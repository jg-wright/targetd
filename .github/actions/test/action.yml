name: Test

description: Run checks and tests

runs:
  using: composite
  steps:
    - shell: bash
      run: deno check --frozen
    - shell: bash
      run: deno lint
    - shell: bash
      run: deno fmt --check
    - shell: bash
      run: deno task test
    - shell: bash
      run: deno publish --dry-run
